[alias]
	br = branch
	ci = commit
	co = checkout
	fp = fetch --prune --all
	graph = log --graph --decorate --oneline --all
	st = status -sb
[color]
	ui = auto
[core]
	editor = vim
[push]
	default = matching
[rerere]
	enabled = 1

# vim: ft=gitconfig
