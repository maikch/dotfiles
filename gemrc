# You probably don't want to run '--no-rdoc --no-ri' for *every* gem command.
# That's why it's specified only for *install* and *update* only.
install: --no-rdoc --no-ri
update: --no-rdoc --no-ri

# vim: ft=yaml
